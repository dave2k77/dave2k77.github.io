{
  "name": "davian-chin-personal-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/validate-content.mjs && vite build && node scripts/build-articles.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "markdown-it": "^15.0.2",
    "yaml": "^2.9.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "vite": "^8.3.1"
  }
}
